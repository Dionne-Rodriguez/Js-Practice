Count the number of occurrences of each character
and return it as a list of tuples in order of appearance.
const str = 'abracadbra'
const orderedCount = (str) => {
const charArr = []
 for (let i = 0; i < str.length ; i++);{
  if(charArr.includes(text[i])){
    continue;
    charArr.push(str[i]);
  }else{
    charArr.push(str[i])
  }
 }
}
charArr.forEach((str) =>{
  tupleArr.push
}) 
